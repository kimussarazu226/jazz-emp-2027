# JAZZ EMP vol.9 Official Website

- `template.html` — 編集はこのファイルだけ。`__KV__` はキービジュアルの参照に置き換わる
- `build.py` — `python3 build.py` で `index.html` を生成
- `assets/` — キービジュアル（WebP）ほか画像
- ローカル確認: `python3 -m http.server 8787` → http://localhost:8787
